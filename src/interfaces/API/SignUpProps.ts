export interface signUpRequestProps {
    username: string;
    email: string;
    password: string;
}

export interface signUpResponseProps {
    username: string;
    email: string;
    password: string;
}